# syntax=docker/dockerfile:1.7

FROM rust:1.96-bookworm AS builder

WORKDIR /workspace

RUN apt-get update \
    && apt-get install -y --no-install-recommends protobuf-compiler libprotobuf-dev \
    && rm -rf /var/lib/apt/lists/*

# Keep the build bounded on developer machines. The repository's
# rust-toolchain.toml selects the pinned Rust toolchain.
ENV CARGO_BUILD_JOBS=8 \
    CARGO_TERM_COLOR=always

COPY . .

RUN cat crates/dbt-adapter/src/sql_types.rs \
    | grep -E 'Spark =>|Spark, 1|Spark, \.\.=0|Spark \} else' \
    || (echo "Expected Spark mappings were not copied into the build context" && exit 1)

RUN --mount=type=cache,target=/usr/local/cargo/registry,sharing=locked \
    --mount=type=cache,target=/usr/local/cargo/git,sharing=locked \
    --mount=type=cache,target=/workspace/target,sharing=locked \
    rustup show active-toolchain \
    && cargo build -p dbt-sa-cli --release -j 8 \
    && install -Dm755 target/release/dbt /out/dbt

FROM scratch AS output

COPY --from=builder /out/dbt /dbt
